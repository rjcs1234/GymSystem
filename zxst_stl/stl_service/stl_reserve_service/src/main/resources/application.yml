server:
  port: 8082
spring:
  application:
    name: 
  datasource:
    driver-class-name: 
    username: 
    password: 
    url:
    type:

  data:
    redis:
      host:
      port:
      password: 

  mvc:
    hiddenmethod:
      filter:
        enabled: 
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: 

eureka:
  client:
    service-url:
      defaultZone:
